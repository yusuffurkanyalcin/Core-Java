## Why do we use create this design pattern by using ENUM type ? 
  - less writing code 
  - instance reference security
 ## Also
  - lazy creation is not consistent 
  - eager creation wastes memory
